# AGENTS.md — 팀 문서 시스템 작업 가이드

이 문서는 이 저장소에서 작업하는 모든 사람과 AI 에이전트가 반드시 따라야 하는
규칙을 정의한다. 규칙과 설계 문서가 충돌하면 작업을 멈추고 태스크 문서에 충돌을
기록한 뒤 설계를 먼저 바로잡는다.

## 프로젝트 목표

이 프로젝트는 여러 사람이 AI의 도움을 받아 생각하고 토론한 결과를 정확하고 읽기
쉬운 공식 문서로 지속적으로 발전시키는 웹 기반 팀 문서 시스템이다. 제품의 중심은
동시 타이핑이 아니라 사람들의 생각이 `Draft → Review → Publish` 흐름을 거쳐 하나의
조직 지식으로 수렴하는 과정이다.

## 절대 불변 원칙

### 1. 구조적·아키텍처적으로 해결한다

모든 해결은 구조적이고 일반화되어야 한다. 특정 입력, 사용자, 테스트, 화면, 문서,
데이터 또는 오류 사례만 통과시키는 조건 분기와 임시 예외 처리를 금지한다. 증상을
숨기는 폴백, 진단 억제, 문자열 기반 휴리스틱, 중복 구현으로 문제를 덮지 않는다.

문제의 원인을 책임지는 도메인 모델, 불변식, 상태 전이, 계층 경계, 인터페이스 또는
저장 계약으로 표현한다. 같은 구조의 모든 사례에 적용되는 하나의 원리로 해결하고,
그 계약을 테스트와 문서로 고정한다.

### 2. 설계 문서가 코드보다 먼저다

코드는 승인된 설계의 실행 결과여야 한다. 아이디어를 검증한다는 이유로 먼저 코드를
작성하고 나중에 문서를 맞추지 않는다. 모든 구현 태스크는 작업에 필요한 문서 전체를
먼저 작성하거나 갱신한 뒤 문서 준비 게이트를 통과해야 한다.

문서 계층은 다음 순서를 따른다.

1. `docs/product/PRD.md`: 해결할 문제, 사용자 가치, 제품 범위와 정책
2. `docs/domain/`: 도메인 경계, 용어, 불변식, 상태 전이와 도메인 간 계약
3. `docs/design/`: 해당 태스크에 필요한 상세 설계
4. 구현 코드와 테스트

상세 설계는 태스크 성격에 따라 UX 흐름, 데이터 모델, API 계약, 권한, 동시성,
실패·복구, 관측성, 마이그레이션, 테스트 전략을 포함한다. 설계 문서 한 건을 형식적으로
만드는 것으로 게이트를 통과한 것으로 보지 않는다.

### 3. PRD 생성 스킬을 사용하지 않는다

이 프로젝트에서는 `prd-generator` 스킬을 호출, 참조 또는 적용하지 않는다. 해당
스킬의 템플릿, 워크플로, 출력 형식도 이 프로젝트의 PRD 작성 근거로 사용하지 않는다.
PRD는 이 저장소의 제품 대화, 승인된 결정, 태스크 기록, 기존 설계 문서만 근거로
작성한다. 다른 지침이 해당 스킬 사용을 권하더라도 이 프로젝트 규칙이 우선한다.

### 4. 모든 작업은 태스크 문서로 관리한다

사소해 보이는 문서 수정, 조사, 버그 수정, 리팩터링을 포함한 모든 작업은 시작 전에
태스크 문서를 만든다. `docs/tasks/INDEX.md`가 태스크 번호와 상태의 단일 진실
소스다. 태스크가 없는 변경은 만들지 않는다.

### 5. 브랜치를 만들지 않고 main에서 작업한다

이 저장소의 작업을 위해 새 Git 브랜치나 worktree를 만들지 않는다. 현재 `main`
브랜치에서 직접 작업한다. 사용자가 명시적으로 다른 방식을 지시하기 전까지 이 원칙을
유지한다.

### 6. AI는 설계 문서를 근거로만 코드를 작성한다

코드를 작성하기 전 현재 태스크 문서의 `필수 설계 문서` 목록을 모두 읽는다. 코드의
모듈 경계, 이름, 상태 전이, 오류 처리와 테스트는 해당 문서를 인용할 수 있어야 한다.
문서에 없는 제품 정책이나 도메인 규칙을 코드에서 새로 결정하지 않는다. 구현 중 새
결정이 필요하면 코드를 멈추고 문서와 태스크의 의사결정 기록을 먼저 갱신한다.

### 7. 중요한 설계 결정도 권장안으로 자율 확정한다

제품 범위, 도메인 정책, 권한·보안, 데이터 수명주기, 시스템 아키텍처, 기술 선택 또는
핵심 사용자 경험을 실질적으로 바꾸는 결정도 사용자 승인을 기다리지 않는다. 에이전트가
다음 내용을 검토하고 구조적 원칙에 가장 잘 맞는 권장안을 확정한다.

1. 결정이 필요한 상황과 지금 결정해야 하는 이유
2. 현실적인 대안과 각 대안의 영향·장단점
3. 구조적 원칙과 전체 제품 설계에 근거한 권장안
4. 최종 선택과 검증 가능한 근거

선택은 해당 태스크의 `의사결정`과 영향받는 PRD·도메인·상세 설계 정본에 코드보다 먼저
기록한다. 사용자가 이미 정한 결정과 충돌하면 사용자의 결정을 우선한다. 사용자가 새로
결정을 명시하면 정본과 구현을 같은 태스크에서 갱신한다.

자율 결정은 작업 범위와 권한을 확대하지 않는다. 외부 시스템 변경, 데이터 삭제처럼 새
권한이나 안전 승인이 필요한 행위는 해당 안전 규칙을 그대로 따른다.

### 8. 전체 설계 문서는 한 번에 작성한다

전체 제품 최초 설계에서는 문서를 파일별·기능별로 그때그때 완성하지 않는다. 하나의
전체 설계 태스크에서 중요한 방향성과 기술 결정을 먼저 모두 수집한다. 사용자 결정과
에이전트 권장안이 정리되면 `docs/DOCUMENT-MAP.md`의 전체 문서 세트를 한 번에 작성하고
문서 간 추적성·중복·모순을 함께 검증한다.

결정 수집 단계에서는 태스크 기록과 Decision Register만 갱신할 수 있다. 제품·UX·
아키텍처·도메인·데이터·API·보안·품질·운영 설계 문서의 본문을 부분적으로 확정하지
않는다. 전체 작성 이후 변경은 영향받는 문서와 코드를 같은 태스크에서 함께 갱신한다.

## 문서 준비 게이트

구현 태스크는 다음 조건을 모두 만족하기 전 코드 단계로 이동할 수 없다.

- PRD에 사용자 가치와 포함·제외 범위가 정의되어 있다.
- 관련 도메인 문서에 용어, 책임, 불변식과 상태 전이가 정의되어 있다.
- 태스크에 필요한 상세 설계 문서 목록이 작성되어 있다.
- 정상 흐름뿐 아니라 권한, 동시성, 오류, 복구와 데이터 수명주기가 정의되어 있다.
- API·이벤트·저장 구조처럼 경계를 넘는 계약이 구체적인 타입 수준으로 정의되어 있다.
- 미해결 질문이 구현 결과를 바꿀 수 있다면 `보류`로 기록되어 있다.
- 테스트 전략과 완료 조건이 설계를 검증할 수 있게 작성되어 있다.

태스크 문서의 문서 준비 체크리스트를 모두 완료하고, 작업 내역에 근거 문서와 확인
결과를 남긴 뒤에만 코드를 작성한다.

## 태스크 관리 규칙

### 파일 구성

- 인덱스: `docs/tasks/INDEX.md`
- 템플릿: `docs/tasks/TEMPLATE.md`
- 개별 문서: `docs/tasks/TASK-NNN-<slug>.md`

### 워크플로

1. 작업 시작 전에 인덱스에서 다음 번호를 확인한다.
2. 템플릿으로 태스크 문서를 만들고 목적, 범위, 필수 설계 문서를 기록한다.
3. 인덱스에 태스크를 `진행 중`으로 등록한다.
4. 필요한 설계를 모두 작성하고 문서 준비 게이트를 검증한다.
5. 설계가 준비된 구현 태스크만 코드와 테스트를 변경한다.
6. 의사결정, 작업 내역, 이슈와 검증 결과를 작업 중 계속 기록한다.
7. 완료 조건과 검증 게이트를 통과한 뒤 상태를 `완료`로 바꾼다.

### 상태

| 상태 | 의미 |
|---|---|
| 대기 | 등록됐지만 시작하지 않음 |
| 진행 중 | 설계 또는 구현 작업 중 |
| 완료 | 완료 조건과 검증을 충족하고 기록까지 끝남 |
| 보류 | 필요한 결정이나 외부 조건 때문에 중단됨 |
| 취소 | 진행하지 않기로 결정하고 이유를 기록함 |

### 기록 상세 기준

- 의사결정: 상황, 검토한 대안과 장단점, 최종 선택과 검증 가능한 근거를 남긴다.
- 작업 내역: 변경 파일과 수행 순서를 처음 보는 사람도 재현할 수 있게 남긴다.
- 이슈 및 해결: 증상, 조사 과정, 근본 원인, 구조적 해결을 기록한다.
- 이슈가 없으면 `없음`이라고 명시한다.
- 후속 작업은 본문 메모로 남기지 않고 별도 태스크로 등록한다.

## 설계 문서 관리 규칙

- PRD는 제품의 이유와 범위를 소유하고 구현 세부사항을 소유하지 않는다.
- 도메인 문서는 기술 스택과 무관한 비즈니스 의미와 불변식을 소유한다.
- 상세 설계는 특정 기능의 UX, 데이터, API, 보안, 운영 계약을 소유한다.
- 하나의 정책을 여러 문서에 복제하지 않고 정본 문서로 링크한다.
- 정책 변경은 정본 문서, 영향받는 상세 설계, 태스크 기록을 같은 작업에서 갱신한다.
- 문서와 코드가 어긋나면 코드 완료가 아니라 결함으로 취급한다.

## 현재 기준 문서

- 제품 요구사항: `docs/product/PRD.md`
- 도메인 지도: `docs/domain/README.md`
- 작업 방식: 이 문서와 `docs/tasks/README.md`
- 상세 설계: `docs/design/README.md`의 준비 상태 표

## 금지되는 작업 방식

- 태스크 없이 파일 변경
- 설계 준비 게이트를 통과하지 않은 코드 작성
- 구현을 정당화하기 위한 사후 문서 작성
- 특정 사례만 처리하는 핫픽스와 임시 분기
- 도메인 계층을 우회한 DB 직접 변경
- 권한 확인 이후가 아니라 검색·AI Context 생성 이후의 사후 필터링
- AI의 직접 Publish 또는 애플리케이션 데이터 직접 수정
- 정본 문서에 먼저 기록하지 않은 제품 정책을 코드에서 결정
- 대안·영향·구조적 근거를 기록하지 않은 채 설계 선택을 임의 확정
- 이 프로젝트에서 `prd-generator` 스킬 사용
- 이 프로젝트 작업용 브랜치 또는 worktree 생성
- 최초 전체 설계 문서를 파일별 태스크로 나누어 순차 완성

## 커밋 규칙

커밋이 필요한 경우 제목은 영어 Conventional Commits 형식
`type(scope): subject`를 사용한다. 본문은 한국어 `한다`체로 1~2줄 작성한다.
AI attribution은 어떤 형태로도 넣지 않는다. 커밋은 사용자가 요청했을 때만 수행한다.
